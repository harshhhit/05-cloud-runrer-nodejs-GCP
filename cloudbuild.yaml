steps:
  - name: 'gcr.io/clean-mountain-395906/ubuntu:20.04'
    entrypoint: bash
    args:
      - '-c'
      - |
        << 'EOF'
          apt-get update
          apt-get install -y nodejs npm
          npm install snyk@latest -g
            EOF

